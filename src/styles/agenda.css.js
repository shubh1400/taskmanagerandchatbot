import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#f2f2f2',
    },
    emptyText: {
        textAlign: 'center',
        marginTop: 10,
        color: '#555',
    },
    card: {
        backgroundColor: '#fff',
        borderColor: "#000000",
        borderWidth: 1,
        padding: 15,
        marginHorizontal: 15,
        marginBottom: 10,
        borderRadius: 8,
        marginVertical: 10,
    },
});